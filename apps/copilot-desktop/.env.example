# Dev cert signing env (T-1.5.1, Sprint 1.5)
# Real value lives in .env (gitignored) — never commit real password
# Self-signed dev cert only; prod cert arrives in Sprint 1.6+
CSC_KEY_PASSWORD=changeme-dev-only
