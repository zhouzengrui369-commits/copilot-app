# Sprint 1.2 · T-1.2.2 · 知识图谱 2D 渲染（sigma.js + 节点交互）

> PM: Mavis | 创建: 2026-07-09 14:38 (UTC+8)
> Worktree: `/Users/njx/openclaw/copilot.wt-T122/wt-T122/`
> 分支: `sp1.2-T-1.2.2` (from main HEAD)
> Agent: coder (sub-agent β)
> 配套: plan.md v6.2 §2.2 / goal.md v6.2 / rules.md v6.2 / delivery.md v6.2
> **30min cap 必看**：2 wave 拆

---

## 1. Goal

为 copilot app 创建 **知识图谱 2D 可视化层**：用 sigma.js 渲染 KG 节点 + 边，支持点击 / 拖拽 / 缩放 / 筛选 / 搜索。

验收信号（plan.md §2.2 T-1.2.2）：
- **100 节点流畅渲染 ≥ 30 FPS**
- 点击 / 拖拽 / 缩放正常
- 筛选（按 type / tag） + 搜索（按 name）可用

---

## 2. Scope（Allowed）

新建/修改 文件**仅限**：
- `apps/copilot-desktop/src/components/KnowledgeGraph/`
  - `index.tsx`（主入口）
  - `SigmaCanvas.tsx`（sigma.js 包装）
  - `NodeInteraction.tsx`（点击 / 拖拽 / 缩放）
  - `FilterPanel.tsx`（按 type / tag 筛选）
  - `SearchBox.tsx`（按 name 搜索）
  - `useKgData.ts`（从 packages/kg 拉数据 hook）
  - `styles.module.css`
- `apps/copilot-desktop/src/components/KnowledgeGraph/__tests__/`
- `apps/copilot-desktop/src/components/KnowledgeGraph/SELF-VERIFY-T-1.2.2.md`
- `screenshots/T-1.2.2/` ≥ 3 张
- `outputs/T-1.2.2/deliverable.md`
- `board.md`

---

## 3. Forbidden

- ❌ 不动 `apps/web/` / `apps/server/` / 其他 sub-agent worktree
- ❌ 不动 packages/kg 内部实现（只消费 API）
- ❌ 不动 main 分支
- ❌ **不**用 3D 库（three.js / 3d-force-graph 是 Sprint 2 T-2.1）
- ❌ **不**阻塞主线程渲染（必须用 sigma.js 的 webgl 后端或 RAF）

---

## 4. Tech Stack

| 层 | 选型 | 备注 |
|---|---|---|
| 渲染 | sigma.js v3+ | WebGL 后端 + graphology |
| 数据 | graphology | 内存图数据结构 |
| 交互 | sigma.js 内置 | 点击 / 拖拽 / 缩放 |
| 数据源 | packages/kg query API | 节点 + 边 JSON |
| 状态 | React hooks | useKgData / useFilter |
| 样式 | CSS modules | 跟 Sprint 1.1 T-1.1.1 同款 |

---

## 5. Wave 拆分（30min cap 必看 · 钉子 #14 强化）

### wave 1 (≤25min): sigma.js 基础渲染
- 集成 sigma.js v3+ 到 Electron renderer
- 接入 packages/kg query API（节点 + 边）
- 实现 SigmaCanvas 组件（100 节点 30 FPS 验证）
- 截图：100 节点渲染图（≥ 1 张）

### wave 2 (≤25min): 交互 + 筛选 + 搜索
- 点击节点 → 触发回调（T-1.2.3 详情预览对接）
- 拖拽 / 缩放正常
- FilterPanel（按 type / tag 筛选）
- SearchBox（按 name 模糊搜索）
- 截图：交互状态（≥ 2 张：筛选 + 搜索）

**超时处理**：wave 1 失败 → 整个 T-1.2.2 失败；wave 2 失败 → 渲染部分保留 + 交互标 PARTIAL。

---

## 6. 必交付（Deliverable）

### 6.1 代码
- `apps/copilot-desktop/src/components/KnowledgeGraph/index.tsx`
- `apps/copilot-desktop/src/components/KnowledgeGraph/SigmaCanvas.tsx`
- `apps/copilot-desktop/src/components/KnowledgeGraph/NodeInteraction.tsx`
- `apps/copilot-desktop/src/components/KnowledgeGraph/FilterPanel.tsx`
- `apps/copilot-desktop/src/components/KnowledgeGraph/SearchBox.tsx`
- `apps/copilot-desktop/src/components/KnowledgeGraph/useKgData.ts`
- `apps/copilot-desktop/src/components/KnowledgeGraph/__tests__/`

### 6.2 文档 + 截图
- `SELF-VERIFY-T-1.2.2.md`（3 验收信号 + 截图路径 + FPS 数据）
- `outputs/T-1.2.2/deliverable.md`（VERDICT + 已跑命令 + 截图 + known limitations）
- `board.md` append done 行
- `screenshots/T-1.2.2/100-nodes-30fps.png` (wave 1)
- `screenshots/T-1.2.2/filter-by-type.png` (wave 2)
- `screenshots/T-1.2.2/search-by-name.png` (wave 2)

---

## 7. 验证（PM 用）

```bash
cd /Users/njx/openclaw/copilot.wt-T122/wt-T122
git log --oneline -3                    # 2 wave commit
git status --porcelain                  # 应为空
ls apps/copilot-desktop/src/components/KnowledgeGraph/*.tsx | wc -l  # ≥ 6
ls screenshots/T-1.2.2/*.png | wc -l    # ≥ 3
test -f outputs/T-1.2.2/deliverable.md  # 钉子 #14

cd apps/copilot-desktop
npm install && npm run check && npm run test  # exit 0

# 启动 Electron 跑 cu MCP 验证
npm run dev &                           # 后台启动
sleep 30
# cu MCP desktop_screenshot + desktop_left_click KG 节点
# 验证 100 节点 30 FPS（performance.now 测）
```

---

## 8. 风险（PM discipline #6）

| # | 风险 | 缓解 |
|---|------|------|
| R1 | 100 节点 30 FPS 在低端 Mac 难达标 | sigma.js ForceAtlas2 调参 + 关 anti-aliasing + WebGL |
| R2 | 与 T-1.2.1 KG builder 衔接阻塞 | 等 T-1.2.1 wave 2 后再开始（plan.yaml depends_on） |
| R3 | Electron 集成路径冲突 Sprint 1.1 T-1.1.1 设置面板 | 独立组件目录 KnowledgeGraph/，不污染 |
| R4 | 30min cap 撞 wave 2 交互 | partial accept 渲染部分 + 交互标 PARTIAL |

---

## 9. 决策红线

- **不**阻塞主线程（web worker 或 RAF）
- **不**用 3D 库（Sprint 2 T-2.1 范围）
- **不**污染 packages/kg schema
- **不**改 Sprint 1.1 T-1.1.1 设置面板
