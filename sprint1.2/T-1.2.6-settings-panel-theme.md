# Sprint 1.2 · T-1.2.6 · 设置面板 + 主题（深/浅/自动）+ 模型 API 配置

> PM: Mavis | 创建: 2026-07-09 14:38 (UTC+8)
> Worktree: `/Users/njx/openclaw/copilot.wt-T126/wt-T126/`
> 分支: `sp1.2-T-1.2.6` (from main HEAD)
> Agent: coder (sub-agent ζ)
> 配套: plan.md v6.2 §2.2 / goal.md v6.2 / rules.md v6.2 / delivery.md v6.2
> **30min cap 必看**：单 wave（1-2h 估时）

---

## 1. Goal

为 copilot app 扩展 **设置面板**：主题切换（深色 / 浅色 / 自动）+ 模型 API 配置（minimax / OpenAI / Claude / 自托管）+ 重置功能。

验收信号（plan.md §2.2 T-1.2.6）：
- 所有设置项可保存可重置
- 主题切换实时生效
- 模型 API 切换可工作（config 写入 + LLM client 重连）

---

## 2. Scope（Allowed）

新建/修改 文件**仅限**：
- `apps/copilot-desktop/src/components/Settings/`
  - `index.tsx`（设置主面板 — 继承 Sprint 1.1 T-1.1.1）
  - `ThemeSelector.tsx`（深 / 浅 / 自动）
  - `ModelApiConfig.tsx`（minimax / OpenAI / Claude / 自托管）
  - `ResetButton.tsx`（重置全部设置）
  - `useSettings.ts`（设置持久化 hook）
  - `styles.module.css`
- `packages/llm-client/src/config.ts`（扩展支持多 provider）
- `packages/llm-client/src/provider/`（新增 OpenAI / Claude / 自托管 provider）
- `apps/copilot-desktop/src/components/Settings/__tests__/`
- `SELF-VERIFY-T-1.2.6.md`
- `screenshots/T-1.2.6/` ≥ 3 张
- `outputs/T-1.2.6/deliverable.md`
- `board.md`

⚠️ **继承 Sprint 1.1 T-1.1.1 设置面板基础**：云备份开关已在，本次主要扩展主题 + 模型 API 配置。**不**改 Sprint 1.1 已实现的云备份开关（决策红线）。

---

## 3. Forbidden

- ❌ 不动 `apps/web/` / `apps/server/` / 其他 sub-agent worktree
- ❌ **不**用 Electron 原生主题 API（用 CSS 变量 + React 状态）
- ❌ **不**支持运行时切换 LLM provider schema（只切换 config，重启后生效）
- ❌ 不动 main 分支
- ❌ **不**改 Sprint 1.1 T-1.1.1 已实现的云备份开关（决策红线：默认 OFF）

---

## 4. Tech Stack

| 层 | 选型 | 备注 |
|---|---|---|
| 主题 | CSS variables + React Context | 实时切换不闪烁 |
| 持久化 | electron-store | 跟 Sprint 1.1 T-1.1.1 同款 |
| LLM provider | minimax m3 (默认) / OpenAI / Claude / 自托管 | config 切换 |
| API 切换 | config 写入 + LLM client reinit | 重启后生效 |

---

## 5. Wave（单 wave · 30min cap 必看）

### wave 1 (≤25min · 1-2h 实际估时)
- ThemeSelector（深 / 浅 / 自动 CSS 变量切换）
- ModelApiConfig（4 个 provider 配置表单 + 切换）
- ResetButton（重置 + 二次确认）
- useSettings hook（electron-store 持久化）
- packages/llm-client 扩展支持 OpenAI / Claude / 自托管
- 单测：5 个 case（主题切换 / 模型切换 / 持久化 / 重置 / LLM reinit）
- 截图：深 / 浅 / 自动 + 模型 API（≥ 3 张）

**超时处理**：超 25min → 立即 commit + PARTIAL + 列剩余。

---

## 6. 必交付

### 6.1 代码
- `apps/copilot-desktop/src/components/Settings/index.tsx`（扩展）
- `apps/copilot-desktop/src/components/Settings/ThemeSelector.tsx`
- `apps/copilot-desktop/src/components/Settings/ModelApiConfig.tsx`
- `apps/copilot-desktop/src/components/Settings/ResetButton.tsx`
- `apps/copilot-desktop/src/components/Settings/useSettings.ts`
- `apps/copilot-desktop/src/components/Settings/__tests__/`
- `packages/llm-client/src/config.ts`（扩展）
- `packages/llm-client/src/provider/openai.ts`（新增）
- `packages/llm-client/src/provider/claude.ts`（新增）
- `packages/llm-client/src/provider/custom.ts`（新增）

### 6.2 文档 + 截图
- `SELF-VERIFY-T-1.2.6.md`
- `outputs/T-1.2.6/deliverable.md`（VERDICT + 4 provider 切换测试）
- `board.md` append done 行
- `screenshots/T-1.2.6/theme-dark.png`
- `screenshots/T-1.2.6/theme-light.png`
- `screenshots/T-1.2.6/model-api-config.png`

---

## 7. 验证（PM 用）

```bash
cd /Users/njx/openclaw/copilot.wt-T126/wt-T126
git log --oneline -1
git status --porcelain
ls apps/copilot-desktop/src/components/Settings/*.tsx | wc -l  # ≥ 5
ls packages/llm-client/src/provider/*.ts | wc -l  # ≥ 3 (minimax 已有)
ls screenshots/T-1.2.6/*.png | wc -l    # ≥ 3
test -f outputs/T-1.2.6/deliverable.md

cd packages/llm-client
npm install && npm run check && npm run test  # exit 0

cd ../../apps/copilot-desktop
npm install && npm run check && npm run test  # exit 0

# 主题切换真跑（必跑）
# cu MCP：进设置 → 切深 → 验证 UI 实时变深；切浅 → 验证实时变浅；切自动 → 验证跟随系统
# 模型 API 切换真跑
# 设置 OpenAI provider + 测 1 个 chat 请求 → 验证切换生效
```

---

## 8. 风险

| # | 风险 | 缓解 |
|---|------|------|
| R1 | 主题切换闪烁 | CSS 变量 + 过渡动画 + body class 切换 |
| R2 | LLM provider 切换需要重启 | 文档说明 + UI 提示「切换后需重启」 |
| R3 | 自托管 provider 配错 URL 崩溃 | 输入校验 + 错误捕获 + 友好提示 |
| R4 | 重置按钮误点 | 二次确认弹窗 + 3s 延迟 |
