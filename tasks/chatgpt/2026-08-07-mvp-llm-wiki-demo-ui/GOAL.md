# GOAL — Copilot App MVP llm_wiki + Demo UI Integration

Deliver one bounded, source-verifiable macOS-first Copilot MVP slice that clean-room adapts the previously researched `nashsu/llm_wiki` product architecture and reuses the existing demo-first Electron UI shell without changing Copilot's local-first data truth or introducing a second knowledge stack.

Success for this remote slice means:

- the Knowledge Studio source is implemented on an isolated ChatGPT branch;
- GPLv3 upstream implementation bytes are not copied;
- existing Note/KB/WIKI/KG/RAG/Ask/Todo/Schedule truth remains authoritative;
- the complete Node 24/macOS `copilot-source-gate` passes on the exact PR head;
- the result is integrated only into Draft PR #20 and revalidated there before any MiniMax candidate run;
- no GitHub/CI result is represented as packaged Electron runtime proof.

```text
NOT_RUNTIME_PROOF
MVP_NOT_COMPLETE
NOT_RELEASE_READY
NOT_EXPERIENCE_READY
```
