# GOAL

Deliver an exact-source, macOS-safe, one-process hydrator launcher that cannot pre-create `NATIVE_CACHE_DIR` or `NATIVE_CACHE_RECEIPT`, then return one source-green successor through Draft PR #20 without claiming local runtime/MVP completion.
