# GOAL

Close the R57 bootstrap/runtime repository-root defect without weakening R50/R56 registry closure, retry, network, package/lockfile, Candidate, signing or release boundaries.

Success means the exact R58 source can discover Git-tracked supplemental lock identities when the hydrator module is extracted outside the repository, because repository authority comes from the hydration `--repository` contract rather than module location.
