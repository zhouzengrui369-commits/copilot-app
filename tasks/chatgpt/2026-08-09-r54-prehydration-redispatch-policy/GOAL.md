# GOAL

Create a governance-only successor that distinguishes pre-hydration dispatcher failure from a consumed hydration/Candidate attempt, preserves exact-source and fail-closed boundaries, and defines a durable single-process hydrator execution envelope.

Success means: R54 source gate PASS, no product/R50/package/lockfile/test changes, merge only into Draft PR #20, then final PR #20 source gate PASS before MiniMax resumes.
