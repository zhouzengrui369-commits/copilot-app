# TASK

1. Freeze R53 first/second-dispatch evidence as `FORBIDDEN_REFERENCE_ONLY`.
2. Preserve R50 hydration implementation and all product bytes unchanged.
3. Update exact-object MVP handoff with Tier A/Tier B successor policy.
4. Require new RUN_STAMP and all-new six paths for Tier A same-source redispatch.
5. Require a new source SHA once hydration/native-cache/Candidate state is consumed.
6. Define persistent single-process hydration (`run_in_background=true` preferred; one PID/nohup fallback) with outer allowance >=3600s.
7. Run complete macOS source gate on final stacked head.
8. Merge only into Draft PR #20; run complete source gate again on resulting exact PR #20 head.
9. Do not merge `main`; do not claim runtime/MVP/release/experience readiness.
