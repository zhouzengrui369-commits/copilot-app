# GOAL

Close the R55 npm registry prefetch manifest-to-closure identity gap without weakening deny-network, integrity, retry, Candidate, package/lockfile, or release boundaries, then produce a new source-green successor SHA for local execution.
