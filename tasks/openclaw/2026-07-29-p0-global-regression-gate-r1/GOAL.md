# GOAL

Run one current-source global desktop regression gate after EXP-COP-008 and
readiness repairs, persist machine-readable results, and classify every
remaining failure without changing product or tests.
