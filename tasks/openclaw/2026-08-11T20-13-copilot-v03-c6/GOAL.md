# GOAL — Copilot Shared Knowledge Engine C6 R1

Goal ID: `COPILOT-SKE-V03-C6-R1`
State: `SOURCE_ACTIVE`
Base: `8adf7c596126306df5b7eaa07d31241d66230536`
Program: `ECOSYSTEM-V03-DIGITAL-TWIN-R1`
Contract: `0.3.0-draft`

## Goal

Bind Copilot's real local-first implementation surfaces to the accepted C1–C5 Shared Knowledge Engine contracts without promoting physical storage, renderer IPC, KG rows or RAG chunks into ecosystem APIs, then freeze a source-verified exact Runtime successor for local conformance execution.

## Source scope

Create a desktop main-process conformance adapter/harness over the existing narrow domain ports:

- real NoteDocument -> canonical Source + Knowledge;
- real KG entity/relation DTOs -> canonical Entity + Relation;
- real RAG source-detail hits -> canonical-ID projection/retrieval evidence;
- six projection surfaces retain the same canonical object ID;
- C4 Agent capability read/retrieval/write-proposal is exercised through the same policy contract;
- C5 portable bundle/import plan is exercised against real NoteDocument mapping;
- C5 deletion plan/receipts map actual local lifecycle stages without false COMPLETE;
- restart/rebuild produces stable IDs and deterministic policy/capability identities.

## Runtime successor phase

After source PASS, Parent PM freezes an exact local C6 Runtime authority for MiniMax. The local candidate must use fresh worktree/cache/receipt/task/evidence/runtime/test-data identities and must not reuse R72/R73/R75 assets. Codex remains blocked until MiniMax returns complete artifact/runtime/test-data-bound evidence.

## Hard boundaries

```text
PR20_CHANGE=NO
MAIN_CHANGE=NO
PHYSICAL_SCHEMA_AS_API=NO
AGENT_DIRECT_DB=NO
PACKAGE_LOCK_CHANGE=NO_UNLESS_EXPLICITLY_REQUIRED_AND_RE-GATED
DEPENDENCY_CHANGE=NO_UNLESS_EXPLICITLY_REQUIRED_AND_RE-GATED
D3_ENABLEMENT=NO
SIGNING=NO
NOTARIZATION=NO
RELEASE=NO
REFERENCE_IMPLEMENTATION_CLAIM=NO
```
