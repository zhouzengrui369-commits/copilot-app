# GOAL

Make the final EXP-COP-008 Electron readback assertion compare persisted local
sources as an unordered exact set, independent of UI delimiter or retrieval
rank, then complete the current-source journey.
