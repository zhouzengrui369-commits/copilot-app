# GitHub continuation handoff

## Project truth

- Workspace baseline: root `goal.md`, `plan.md`, `rules.md`, and `delivery.md` v6.2.
- Current truth: root `PROJECT_STATE.yaml`, `PROJECT_STATUS.md`, `TODO.md`, `DECISIONS.md`, and `CHANGELOG.md`.
- Product boundary: local-first macOS Electron MVP. Cloud remains an optional stateless model boundary.
- Current verdict: `BLOCKED / MVP_NOT_COMPLETE / RELEASE_NOT_READY / EXPERIENCE_NOT_READY / NOT_RUNTIME_PROOF`.
- Windows remains Phase 1.1. Mobile, cloud data truth, Remote, Backup, 3D graph, plugins, multi-user, commercial, and major-upgrade scope remain deferred.

## Remote product source

The consolidated remote macOS MVP source is:

```text
chatgpt/mvp-source-finalization
74454d21910f0c01e0b9d4f8117b4394defe3228
```

It passed `copilot-source-gate` run `30784565586`, all 17 steps. It includes grounded Ask/source truth, Ask → source → return continuity, canonical Todo/Unscheduled/editing source, local-first persistence, app-embedded local-ASR source/package contracts, strict coverage, production SBOM, and exact Electron discovery `113 tests in 9 files`.

That result is source evidence only.

## Local candidate incident

MiniMax Code executed the exact source once:

- exact-object authority PASS;
- registry-only hydration PASS;
- focused cache tests `34/34` PASS;
- candidate source contracts `68/68` PASS;
- dry-run PASS;
- candidate execution count: one;
- blocker: `BLOCKED_NPM_OFFLINE_INSTALL_FAILED` at Gate 2;
- source changes: none.

The old hydration disabled lifecycle scripts. The candidate enabled them under deny-network. `better-sqlite3` could not obtain a prebuild or Node headers. Old cache, receipt, worktrees, evidence, artifacts, and runtime placeholders are reference-only and cannot be reused.

## Active R45 repair

The active branch is:

```text
chatgpt/gate2-native-cache-hydration
```

It introduces:

- `scripts/candidate-r30/native-cache-policy.mjs`;
- `scripts/candidate-r30/native-cache-runtime.mjs`;
- `scripts/candidate-r30/npm-native-cache-hydrate.mjs`;
- exact owner authority `OWNER_APPROVAL_FOR_BOUNDED_NATIVE_TOOLCHAIN_CACHE_HYDRATION`;
- exact package-lock lifecycle package-set validation;
- localhost CONNECT proxy and source-defined official-host allowlist;
- inherited credential/config/mirror stripping;
- lifecycle-enabled build-from-source hydration;
- online Electron 38 arm64 native rebuild;
- deny-network full lifecycle offline install proof;
- deny-network Electron arm64 native rebuild proof;
- Electron executable cache proof;
- every cache-file and aggregate SHA-256 receipt;
- candidate Gate 2 receipt/cache revalidation and post-install cache immutability proof.

Candidate commands remain deny-network. No automatic online retry exists.

## Role boundary

- ChatGPT: bounded GitHub source repair, tests, Draft PR, and MiniMax contract.
- MiniMax Code: exact-SHA new hydration and one candidate execution in clean detached worktrees; no source repair.
- Codex: independent packaged Electron product-experience/runtime acceptance on the same source/artifact/runtime identity.
- Owner: explicit hydration authority, Human Owner Gate, and release decision.

## Repository safety

- Repository remains private.
- Never commit credentials, signing/notary material, runtime databases, raw local logs, build outputs, cache receipts, evidence packages, screenshots, or local task transcripts.
- Never reuse a stale candidate identity after a source change.
- Never use `git push --force`, broad process kill, candidate network fallback, or hidden lifecycle-script bypass.
- Before every push, inspect the exact diff and run a redacted secret scan.

## Next action

Pass the complete source gate on the final R45 head. Freeze and externally report that exact full SHA. Then, and only then, MiniMax Code may perform one explicitly Owner-approved bounded native-toolchain hydration and one new candidate attempt with entirely new cache, receipt, worktree, evidence, artifact, and runtime identities. Codex starts only after a complete internally consistent MiniMax package exists.
