# GitHub Actions import guard

These workflows are preserved but intentionally inactive in the initial private GitHub snapshot.

Before enabling them, review triggers, secrets, runner cost, Windows availability, and release permissions. Enable only by moving the reviewed workflow files back to `.github/workflows/` in a separate, intentional commit.
